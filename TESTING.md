<!-- VIBETAGS-START -->
These guardrails apply to test code. Read them before changing a test, a fixture or a test helper.
Safety guardrails on test code (locked, core, privacy, ignore, audit, secure) are not repeated here: they stay in the always-loaded instruction files.

# AUTO-GENERATED AI RULES
# Generated by VibeTags | https://github.com/PIsberg/vibetags
# Do not edit manually.

## CONTEXTUAL RULES
- `se.deversity.vibetags.annotations.AnnotationDefinitionsTest.TestContextClass`: Focus on test.
- `se.deversity.vibetags.annotations.AnnotationDefinitionsTest.testAIContextCanBeUsedOnMethods()`: Focus on Test focus. Avoid Test avoids.
- `se.deversity.vibetags.processor.internal.GuardrailFileWriterLogContractTest`: Focus on The event names and reason= values asserted here are the writer's published contract, not test scaffolding: people grep build logs for write.skip and reason=cache-unchanged. Add a case when you add an event. Avoid Renaming or merging an asserted event to tidy the assertions; that is a breaking change for every consumer parsing a build log, and nothing else catches it.

## IMPLEMENTATION TASKS
The following elements are drafts that need implementation:

- **se.deversity.vibetags.annotations.AnnotationDefinitionsTest.testAIDraftCanBeUsedOnMethods()**: Test instructions

## ⚡ PERFORMANCE CONSTRAINTS
Hot-path elements — never introduce O(n²) or worse. Always reason about complexity before proposing changes.

- **se.deversity.vibetags.annotations.AnnotationDefinitionsTest.TestPerformanceClass**: O(n) max
- **se.deversity.vibetags.annotations.AnnotationDefinitionsTest.testAIPerformanceCanBeUsedOnMethods()**: O(1) required

## 🔐 CONTRACT-FROZEN SIGNATURES
Internal logic may be modified, but never change method names, parameter types, parameter order, return types, or checked exceptions.

- **se.deversity.vibetags.annotations.AnnotationDefinitionsTest.TestContractClass**: Stable public API
- **se.deversity.vibetags.annotations.AnnotationDefinitionsTest.testAIContractCanBeUsedOnMethods()**: Test contract

## 🧪 TEST-DRIVEN REQUIREMENTS
Changes to the following elements MUST be accompanied by a matching test update in the same response.

- **se.deversity.vibetags.annotations.AnnotationDefinitionsTest.TestTestDrivenClass**: Coverage goal: 100%. Framework: JUNIT_5.
- **se.deversity.vibetags.annotations.AnnotationDefinitionsTest.testAITestDrivenCanBeUsedOnMethods()**: Coverage goal: 90%. Framework: JUNIT_5, MOCKITO.

## 🧵 THREAD-SAFE BY DESIGN
These elements are explicitly designed to be thread-safe. Preserve the synchronization invariant on every change.

- **se.deversity.vibetags.processor.NewAnnotationsV3DefinitionTest.TS**: Strategy: LOCK_FREE

## ❄️ IMMUTABLE TYPES
The following types are immutable. Do not introduce non-final fields, setters, or mutating methods.

- **se.deversity.vibetags.processor.NewAnnotationsV3DefinitionTest.IM**: immutable type

## ⚠️ DEPRECATED ELEMENTS
Do not extend these elements. Suggest migrating callers to the listed replacement.

- **se.deversity.vibetags.processor.NewAnnotationsV3DefinitionTest.DEP**: Replaced by: `x.NewType`. Migrate any caller to the replacement.

## 📡 OBSERVABILITY INSTRUMENTATION
These elements carry instrumentation watched by dashboards/alerts. Do not remove or rename without flagging.

- **se.deversity.vibetags.processor.NewAnnotationsV3DefinitionTest.OB**: Metrics: m.

## 📜 REGULATORY COMPLIANCE
These elements implement specific compliance clauses. Document compliance impact for every change.

- **se.deversity.vibetags.processor.NewAnnotationsV3DefinitionTest.RG**: GDPR Art. 17 — Implements a regulatory requirement. Any change must document its compliance impact.

## 🧪 STRICT TEST ISOLATION
Tests for the following elements must be strictly isolated (no shared mutable state/resources):

- **se.deversity.vibetags.processor.NewAnnotationsV4DefinitionTest.PT**: Strict test isolation required. No shared mutable state or external resource conflicts.
- **se.deversity.vibetags.processor.WriteCacheAsyncTest**: Strict test isolation required. No shared mutable state or external resource conflicts. Reason: Must keep running alone. Real platform threads plus detector instrumentation generate load that reaches the javac-based end-to-end tests beside it, and those fail as flakes far from this file, so the cost never points back here

## 🌉 LEGACY COMPATIBILITY BRIDGE
Do not restructure or refactor structural patterns of these compatibility bridges:

- **se.deversity.vibetags.processor.NewAnnotationsV4DefinitionTest.LB**: Legacy/compatibility bridge. Do not refactor structural patterns; only modify internal business logic as explicitly requested.

## 🏛️ ARCHITECTURAL BOUNDARY CONSTRAINTS
Strict layering must be respected. No illegal boundary crossing references:

- **se.deversity.vibetags.processor.NewAnnotationsV4DefinitionTest.AR**: Belongs to layer: `domain`. Prohibited from referencing: [infra, web]

## 🔌 PUBLIC API SURFACE PROTECTION
Preserve public signatures, Javadoc, and behavior without breaking backwards compatibility:

- **se.deversity.vibetags.processor.NewAnnotationsV4DefinitionTest.PA**: Public API surface. Preserve signature, Javadoc, backwards compatibility, and binary/source stability.

## 🚨 STRICT EXCEPTION HANDLING
Precise and robust exception handling required. Generic exception catching/throwing is prohibited:

- **se.deversity.vibetags.processor.NewAnnotationsV4DefinitionTest.SE**: Strict exception handling required. Catching/throwing generic Exception/Throwable is prohibited.

## 🏷️ STRICT TYPE SAFETY
Type safety must be strictly preserved. Loose or erased types are prohibited:

- **se.deversity.vibetags.processor.NewAnnotationsV4DefinitionTest.ST**: Loose typing (Object, Map<String, Object>, raw types) is prohibited. Enforce type safety.

## 🌐 INTERNATIONALIZATION MANDATE
Do not hardcode user-facing strings. All user-visible text must be localized:

- **se.deversity.vibetags.processor.NewAnnotationsV4DefinitionTest.IT**: Internationalization mandated. User-facing strings must not be hardcoded; retrieve from resources.

## 🛡️ STRICT CLASSPATH INTEGRITY
Dynamic class loading, custom classloaders, and reflection hacks are prohibited:

- **se.deversity.vibetags.processor.NewAnnotationsV4DefinitionTest.SC**: Strict compile-time dependency/classpath constraints. Dynamic loading and reflection hacks prohibited.

## 🗄️ SCHEMA & SERIALIZATION SAFETY
Preserve database/contract schema and serialization compatibility on every change:

- **se.deversity.vibetags.processor.NewAnnotationsV4DefinitionTest.SS**: Schema/serialization safety guaranteed. Prohibit altering data formats or fields without migration plan.

## 🧩 LOAD-BEARING ODDITIES
These look wrong, redundant, or over-defensive and are deliberate. Refactoring is allowed only while the stated invariant survives.

- **se.deversity.vibetags.processor.MultiModuleYamlValidityTest**: Looks removable but is deliberate. Invariant: At least one fixture here gives a single module two sidecars sharing one region id, that is, an annotated main source set and an annotated test source set Breaks if changed: Every reactor fixture annotates main sources only. That is exactly how the duplicate top-level key defect survived: one body per region, so no region ever carried two scaffolds, and all six YAML outputs shipped unparseable
<!-- VIBETAGS-END -->
