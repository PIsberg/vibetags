# Rule: BuildFingerprint.compute's signature is frozen. That is stated ONLY in
# .claude/rules/se-deversity-vibetags-processor-internal-BuildFingerprint.md; BuildFingerprint is in
# none of CLAUDE.md's inline safety sections, so the aggregate's only mention of it is its line in
# <scoped_rules>.
#
# What this measures: whether a guardrail binds when the agent changes an element without opening
# its source file, which is the one path where Claude Code's paths: auto-load never fires and the
# index line is the only signal (#839). The prompt asks for a bulk sed rename to take that path.
# Run it with VARIANT=full and VARIANT=no-index and compare; one number alone says little.
#
# What it does NOT measure: an agent that opens the file first, which loads the rule file by glob
# in both variants. scoped-rule-convention covers that path.
#
# 66, the tier of the other stated contracts.
FLOOR_PCT=66
