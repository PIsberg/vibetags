<!-- VIBETAGS-START -->
<!-- VIBETAGS-MODULE: app -->
# GEMINI AI INSTRUCTIONS
# Generated by VibeTags | https://github.com/PIsberg/vibetags


## CONTINUOUS AUDIT REQUIREMENTS
You are acting as a Senior Staff Engineer. Whenever you write code for the files listed below, you must ensure your completions and chat responses strictly prevent the listed vulnerabilities:

File: `com.example.indexed.app.DocumentService`
Critical Vulnerabilities to Prevent:
- Path Traversal
- Insecure Deserialization

## IMPLEMENTATION TASKS
The following elements are drafts that need implementation:

- `com.example.indexed.app.DocumentImportJob.importAll(java.lang.String)`: Implement resumable import: checkpoint every 1000 rows and restart from the last checkpoint

## CONTRACT-FROZEN SIGNATURES
Internal implementation may be changed, but MUST NOT alter method names, parameter types, parameter order, return types, or checked exceptions:

- `com.example.indexed.app.DocumentService.render(com.example.indexed.core.DocumentModel)`: Public service surface consumed across module boundaries

## TEST-DRIVEN REQUIREMENTS
Changes to the following elements MUST be accompanied by matching test code in the same response:

- `com.example.indexed.app.DocumentService`: Coverage goal: 100%. Framework: JUNIT_5. Test file: src/test/java/com/example/indexed/app.

## OBSERVABILITY INSTRUMENTATION
The following elements emit metrics, traces, or log statements watched by dashboards. Preserve every instrumentation point:

- `com.example.indexed.app.DocumentSearchView.renderRow(java.lang.String)`: Metrics: search.render.count. Traces: search.render. Logs: search.render.slow. Note: Renaming a metric breaks the search dashboard and its alerts

## STRICT TEST ISOLATION
Tests for these elements must run in complete isolation without sharing mutable state:

- `com.example.indexed.app.DocumentImportJob`: Strict test isolation required. No shared mutable state or external resource conflicts. Reason: Each import runs against its own temporary directory and shares no state

## LEGACY COMPATIBILITY BRIDGE
Do not restructure compatibility bridges. Only modify business logic:

- `com.example.indexed.app.DocumentImportJob.mapLegacyColumn(java.lang.String)`: Legacy/compatibility bridge. Do not refactor structural patterns; only modify internal business logic as explicitly requested. Reason: Translates the pre-2020 column names; deleted once the last tenant is migrated

## PUBLIC API SURFACE PROTECTION
Preserve public signatures, Javadoc, and backwards compatibility:

- `com.example.indexed.app.DocumentSearchView`: Public API surface. Preserve signature, Javadoc, backwards compatibility, and binary/source stability. Reason: Returned from the public search endpoint; the field names are the wire format

## STRICT EXCEPTION HANDLING
Catching/throwing generic Exception is prohibited. Use precise exceptions:

- `com.example.indexed.app.DocumentSearchView`: Strict exception handling required. Catching/throwing generic Exception/Throwable is prohibited. Reason: Search failures must surface as SearchException, never a raw runtime type

## STRICT CLASSPATH INTEGRITY
Dynamic class loading and reflection hacks are strictly prohibited:

- `com.example.indexed.app.DocumentSearchView`: Strict compile-time dependency/classpath constraints. Dynamic loading and reflection hacks prohibited. Reason: Serialized by the platform's own Jackson; adding a second JSON library changes the output

## IDEMPOTENCY GUARANTEES
These operations must remain idempotent — calling them multiple times must produce the same result:

- `com.example.indexed.app.DocumentService.storageKey(java.lang.String,java.lang.String)`: Idempotency guaranteed. Multiple invocations must produce the same result as one. Reason: Derives the storage key from inputs only

## FEATURE FLAG GATED CODE
These elements are gated behind a feature flag. Never assume the flag is always active:

- `com.example.indexed.app.DocumentImportJob.useV2Pipeline()`: Gated by feature flag: 'import.v2.enabled' (default: false). Preserve the flag check — never assume it is always on.

## SECURITY-CRITICAL CODE
Do not weaken security properties of these elements. Flag any change for security review:

- `com.example.indexed.app.DocumentSearchView`: Security-critical code [Query handling]. Do not weaken security properties. Flag any change for security review.

## ACCESS & CALLS LIMITATIONS
The following elements have strict caller access limits. AI must not invoke them from outside the allowed boundaries:

- `com.example.indexed.app.DocumentSearchView.loadFromIndex(long)`: Only callable by: [com.example.indexed.app.DocumentService]

## SANDBOX & TEST HARNESS EXCLUSION
The following elements are strictly sandbox/test code. Production code must never import or reference them:

- `com.example.indexed.app.DocumentImportJob.reindexEverything()`: Strictly sandbox or test environment only. Production code must never import or invoke. Reason: Writes directly to the index without validation; catastrophic against production data

## MEMORY ALLOCATION BUDGETS
The following elements have strict heap allocation, autoboxing, or garbage budgets. Optimize allocations carefully:

- `com.example.indexed.app.DocumentSearchView.renderRow(java.lang.String)`: Strict memory budget policy: NO_AUTOBOXING. Minimize or prevent runtime allocations.

## DETERMINISTIC PURE FUNCTIONS
The following elements must remain pure functions without side effects or mutations:

- `com.example.indexed.app.DocumentService.storageKey(java.lang.String,java.lang.String)`: Must remain a pure function. Forbid assignments to enclosing state, fields, or static members.

## OPEN-CLOSED EXTENSION PATTERNS
The following elements require extension using polymorphic patterns (Strategy/Visitor). Do not append branch conditionals:

- `com.example.indexed.app.DocumentImportJob`: Designed for extension via strategy/polymorphism. Do not expand conditionals/switch chains. Required Pattern: STRATEGY_PATTERN

## MANDATORY INPUT SANITIZATION
The following parameters/fields must go through strict sanitizers before hitting queries or renderers:

- `com.example.indexed.app.DocumentSearchView.renderRow(java.lang.String)#highlight`: Input parameter/field must be strictly sanitized against injection attacks: [XSS]

## EXPERIMENTAL PROTOTYPE STUBS
Strict QA constraints and tests are relaxed for these elements, but production classes must never import them:

- `com.example.indexed.app.DocumentImportJob`: Experimental prototype class. Strict constraints (test coverage, i18n) are suspended. Stable production code must never depend on it. Reason: Shape of the import pipeline is still being decided; do not build on these types

## 🤖 GENERATED CODE — EDIT THE SOURCE
These elements are machine-generated and hand edits are silently overwritten. Read them freely; never write them. Change the named source and regenerate.

- `com.example.indexed.app.DocumentImportJob.LEGACY_COLUMNS`: Generated from `schema/legacy-import.yaml`. Hand edits are overwritten — edit `schema/legacy-import.yaml` instead, then run `mvn generate-sources`.

## ⛔ BANNED APIs AT THIS ELEMENT
The following APIs compile here but are prohibited. Use the sanctioned replacement instead.

- `com.example.indexed.app.DocumentSearchView.renderRow(java.lang.String)`: Must not use: java.lang.String.format, java.util.Date. Use StringBuilder and java.time instead. (Runs per result row; String.format dominates the profile at this call rate)

## 🧵 THREAD AFFINITY (NOT THREAD-SAFE)
These elements are safe on exactly one thread. Do NOT add locks to "make them thread-safe" — marshal the call onto the required thread instead.

- `com.example.indexed.app.DocumentSearchView.loadFromIndex(long)`: Pinned to any thread except the main/UI thread. NOT thread-safe — adding a lock is the wrong fix. Marshal via SearchExecutor.submit. If violated: The UI thread blocks on index I/O and the app stops painting

## 🔗 MIRRORED — EDIT ALL SITES TOGETHER
These elements are duplicated elsewhere. They may change freely, but a partial change silently desyncs a mirror no compiler checks.

- `com.example.indexed.app.DocumentSearchView`: Editing this requires the same edit at: com.example.indexed.core.DocumentIndexEntry. Projects the index entry field for field; a field added there and not here is invisible to search. Enforced by DocumentIndexEntryTest#projectionCoversEveryField.
<!-- VIBETAGS-MODULE-END: app -->
<!-- VIBETAGS-MODULE: core -->
# GEMINI AI INSTRUCTIONS
# Generated by VibeTags | https://github.com/PIsberg/vibetags


## LOCKED FILES (DO NOT MODIFY)
Do not suggest modifications to the following files:

- `com.example.indexed.core.DocumentModel`: Core document model: structural changes ripple through every module

## CONTEXTUAL RULES
Apply the following context when assisting with these files:

- `com.example.indexed.core.DocumentRetention`: Focus - Retention windows and the legal basis for them. Avoid - Do not shorten a window to make a test pass; the windows are set by the standards named below
## IGNORED ELEMENTS
The following elements must be completely excluded from AI context and completions:

- `com.example.indexed.core.DocumentRetention.cachedExpiryEpochDay`
## PII / PRIVACY GUARDRAILS
The following elements handle Personally Identifiable Information (PII).
Never include their runtime values in logs, console output, external API calls,
test fixtures, mock data, or code suggestions.

- `com.example.indexed.core.DocumentRetention.ownerEmail`: Owner email identifies a natural person; never log it or put it in a suggestion

## CORE FUNCTIONALITY (EXTREME CAUTION)
The following elements are well-tested core components. Make changes with extreme caution:

- `com.example.indexed.core.DocumentIndexEntry`: Sensitivity: high. Note: Index entries are read by every module; a field change is a format change

## PERFORMANCE CONSTRAINTS (HOT PATH)
Never introduce O(n²) complexity into these elements. Always reason about complexity before proposing changes:

- `com.example.indexed.core.DocumentIndexEntry.compareBySequence(com.example.indexed.core.DocumentIndexEntry)`: O(1). Called once per document per query; anything that allocates shows up in the p99

## THREAD-SAFE BY DESIGN
These elements are thread-safe by design — preserve the synchronization invariant on every change:

- `com.example.indexed.core.DocumentIndexEntry`: Strategy: IMMUTABLE. Note: Every field is final; share freely

## IMMUTABLE TYPES
The following types are immutable. Do not introduce non-final fields, setters, or mutating methods:

- `com.example.indexed.core.DocumentModel`: Shared across threads without copies; every field is final

## DEPRECATED ELEMENTS
The following elements are deprecated. Suggest migration to the named replacement for any caller:

- `com.example.indexed.core.DocumentRetention.expiryFromNow(int)`: Replaced by: `expiryEpochDay(long, int)`. Pass the creation day explicitly instead of relying on the system clock (Removal deadline: 2027-01-01)

## REGULATORY COMPLIANCE
The following elements implement compliance clauses. Document compliance impact for every change:

- `com.example.indexed.core.DocumentRetention`: GDPR Art. 5(1)(e) — Storage limitation: documents are kept no longer than the stated purpose requires

## ARCHITECTURAL BOUNDARY CONSTRAINTS
Respect architectural layering. Boundary crossing references are prohibited:

- `com.example.indexed.core.DocumentIndexEntry`: Belongs to layer: `domain`. Prohibited from referencing: [com.example.indexed.app, javax.servlet]

## STRICT TYPE SAFETY
Loose typing is prohibited. Strongly-typed models required:

- `com.example.indexed.core.DocumentIndexEntry`: Loose typing (Object, Map<String, Object>, raw types) is prohibited. Enforce type safety. Reason: Identifiers are typed to stop a title being passed where an id belongs

## INTERNATIONALIZATION MANDATE
User-facing strings must not be hardcoded; retrieve from resources:

- `com.example.indexed.core.DocumentRetention`: Internationalization mandated. User-facing strings must not be hardcoded; retrieve from resources. Reason: Retention notices are shown to users in their own locale; no concatenated sentences

## SCHEMA & SERIALIZATION SAFETY
Modifying schema or data formats without explicit migration plans is prohibited:

- `com.example.indexed.core.DocumentIndexEntry`: Schema/serialization safety guaranteed. Prohibit altering data formats or fields without migration plan. Reason: Persisted to the document store; field order and names are the on-disk format

## FRAMEWORK-FREE DOMAIN ENTITIES
The following elements are pure Domain Models. Do not import Spring, JPA/Hibernate, Jackson, or other framework packages:

- `com.example.indexed.core.DocumentModel`: Pure Domain Model. Banned imports: [Spring, JPA, Hibernate, Jackson, etc.]. No external framework imports permitted.

## SECURE LOGGING MASKING
The following sensitive elements must be masked, hashed, or omitted from log/stdout streams:

- `com.example.indexed.core.DocumentRetention.ownerEmail`: Sensitive variable. Forbid direct logging/printing. Enforce masking policy: HASH

## REQUIRED CHAIN-OF-THOUGHT EXPLANATIONS
Any change made to these elements requires a step-by-step mathematical/architectural proof of correctness in the PR/walkthrough:

- `com.example.indexed.core.DocumentRetention.expiryEpochDay(long,int)`: Requires step-by-step mathematical or logical explanation (Chain-of-Thought) of all changes. Complexity: HIGH

## SUNSET DEPRECATED APIs
Strictly sunset under deprecation. Introducing *new* references or calls to these elements is forbidden:

- `com.example.indexed.core.DocumentRetention.expiryFromNow(int)`: Strictly sunset/deprecated. Forbid any *new* calls or references. JIRA: DOC-4471. Replacement: `java.lang.Object`

## TEMPORARY CODE WORKAROUNDS
Temporary stubs or hacks that must be refactored or removed before their expiration limit:

- `com.example.indexed.core.DocumentRetention.legacyRetentionDays(java.lang.String)`: Temporary logic/workaround. Expires on: 2026-12-31. Reason: Bridges the pre-2026 records that stored a retention class instead of a day count

## 🧩 LOAD-BEARING ODDITIES
These look wrong, redundant, or over-defensive and are deliberate. Refactoring is allowed only while the stated invariant survives.

- `com.example.indexed.core.DocumentIndexEntry.sequence`: Looks removable but is deliberate. Invariant: Sort order of the index depends on this being monotonically increasing Breaks if changed: A caller assigns a sequence lower than one already issued

## 🔗 MIRRORED — EDIT ALL SITES TOGETHER
These elements are duplicated elsewhere. They may change freely, but a partial change silently desyncs a mirror no compiler checks.

- `com.example.indexed.core.DocumentIndexEntry.documentId`: Editing this requires the same edit at: com.example.indexed.app.DocumentSearchView. The search view projects these fields verbatim; adding one here without adding it there hides it from search. Enforced by DocumentIndexEntryTest#projectionCoversEveryField.
<!-- VIBETAGS-MODULE-END: core -->
<!-- VIBETAGS-END -->
