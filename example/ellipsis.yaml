# VIBETAGS-START
# AUTO-GENERATED BY VIBETAGS
# Generated by VibeTags | https://github.com/PIsberg/vibetags
# Ellipsis AI review configuration. Do not edit manually.

version: 1.3
pr_review:
  rules:
    - "`com.example.payment.PaymentProcessor` (locked): Tied to legacy database schema v2.3. Changes will break production payment processing. Contact the payments team before modifying."
    - "`com.example.security.SecurityConfig` (locked): CRITICAL: Security configuration managed by DevOps team. Any changes require security review and approval ticket SEC-XXXX"
    - "`com.example.security.SecurityConfig.getEncryptionAlgorithm()` (locked): Encryption algorithm tied to compliance requirements (PCI-DSS)"
    - "`com.example.security.SecurityConfig.getKeyRotationHours()` (locked): Key rotation period mandated by company policy"
    - "`com.example.security.SecurityConfig.getMaxLoginAttempts()` (locked): Max login attempts set by security team to prevent brute force"
    - "`com.example.security.SecurityConfig.validateToken(java.lang.String)` (locked): Token validation must match auth server exactly. Changes will break all client authentication"
    - "`com.example.service.OrderService.validateOrder(java.util.Map<java.lang.String,java.lang.Object>)` (locked): Order validation implements 47 business rules. Last changed in Q2 2024 after 3-month testing cycle. DO NOT MODIFY without running full test suite."
    - "`com.example.service.OrderService.calculateTax(java.lang.String,double)` (locked): Tax calculation uses Avalara API integration. Credentials and endpoint configuration managed by finance team."
    - "`com.example.service.OrderService.processPayment(java.lang.String,double)` (locked): Payment processing uses Stripe API v2024.10. Changes require PCI compliance review."
    - "`com.example.security.SecurityConfig` (context): Focus - This class is READ-ONLY for AI assistants. Do not suggest modifications.. Avoid - Any changes to encryption algorithms, key sizes, or validation logic"
    - "`com.example.service.InventoryService` (context): Focus - Maintain inventory consistency across concurrent requests. All stock updates must be atomic.. Avoid - Non-atomic read-modify-write sequences, unsynchronized shared state"
    - "`com.example.service.NotificationService` (context): Focus - Implement notification delivery with retry logic and error handling. Avoid - Hard-coded credentials, synchronous blocking calls"
    - "`com.example.service.OrderService` (context): Focus - Maintain transactional integrity. All database operations must use proper transaction management.. Avoid - Raw SQL queries, direct database connections without connection pooling"
    - "`com.example.service.PricingService` (context): Focus - Optimize pricing calculations for accuracy and throughput. Internal algorithms may use any efficient approach.. Avoid - Floating-point arithmetic for monetary values — use BigDecimal internally, but note that the contract-frozen signatures use double for backwards compatibility"
    - "`com.example.strategy.PaymentStrategy` (context): Focus - Follow the Strategy pattern strictly. Each payment method should be a separate strategy class implementing this interface.. Avoid - Monolithic if-else chains, hard-coded payment logic, single class handling all payment types"
    - "`com.example.utils.StringParser` (context): Focus - Optimize for memory usage over CPU speed. Minimize object allocations and avoid creating intermediate string objects.. Avoid - java.util.regex, String.split(), StringBuilder in loops"
    - "`com.example.internal.GeneratedMetadata` (excluded): treat as non-existent"
    - "`com.example.database.DatabaseConnector` (audit): check for SQL Injection, Thread Safety issues"
    - "`com.example.NotificationService` (draft): Implement email sending via SMTP and push notifications via FCM. Ensure retry logic and rate limiting are applied."
    - "`com.example.payment.PaymentProcessor` (draft): Implement support for new crypto payments without breaking legacy flow."
    - "`com.example.service.NotificationService.sendEmail(java.lang.String,java.lang.String,java.lang.String)` (draft): Implement email sending using JavaMail API or similar. Include HTML template support and attachment handling. Add retry logic for transient failures (max 3 retries with exponential backoff)."
    - "`com.example.service.NotificationService.sendSMS(java.lang.String,java.lang.String)` (draft): Implement SMS sending via Twilio or AWS SNS. Include phone number validation. Handle rate limiting (max 10 SMS per minute per user)."
    - "`com.example.service.NotificationService.sendPushNotification(java.lang.String,java.lang.String,java.lang.String)` (draft): Implement push notification using Firebase Cloud Messaging. Support both Android and iOS. Include notification payload customization."
    - "`com.example.service.NotificationService.queueNotification(java.lang.String,java.lang.String,java.lang.String,int)` (draft): Implement a notification queue using a BlockingQueue or similar structure. Support batch processing and priority levels (LOW, MEDIUM, HIGH, CRITICAL)."
    - "`com.example.service.NotificationService.getDeliveryStatus(java.lang.String)` (draft): Implement delivery status tracking. Return status: PENDING, SENT, DELIVERED, FAILED. Include timestamp and error message if failed."
    - "`com.example.service.OrderService.calculateDiscount(java.lang.String,java.lang.String)` (draft): Implement discount calculation supporting: percentage discounts, fixed amount discounts, buy-one-get-one-free, and tiered discounts based on cart value. Apply maximum one discount per order unless overridden by admin."
    - "`com.example.service.OrderService.updateOrderStatus(java.lang.String,java.lang.String)` (draft): Implement order status workflow: CREATED -> PAYMENT_PENDING -> PAYMENT_CONFIRMED -> PROCESSING -> SHIPPED -> DELIVERED. Support status history tracking with timestamps. Allow cancellation only before SHIPPED status."
    - "`com.example.service.OrderService.searchOrders(java.util.Map<java.lang.String,java.lang.String>,int,int)` (draft): Implement order search with filters: date range, status, customer ID, minimum/maximum amount. Support pagination (default 20 items per page). Return results sorted by creation date descending."
    - "`com.example.service.OrderService.generateOrderConfirmation(java.lang.String)` (draft): Generate order confirmation email content including: order summary, itemized list, shipping address, estimated delivery date, and customer support contact information. Support HTML and plain text formats."
    - "`com.example.strategy.PaymentStrategy.executePayment(double)` (draft): Implement payment execution specific to the payment method (credit card, PayPal, cryptocurrency, etc.). Return transaction ID on success."
    - "`com.example.strategy.PaymentStrategy.validatePaymentMethod()` (draft): Validate payment method specific data (card numbers, email addresses, wallet addresses, etc.). Return true if valid, false otherwise."
    - "`com.example.strategy.impl.CreditCardStrategy.executePayment(double)` (draft): Implement credit card payment processing via Stripe or similar payment gateway. Include: card tokenization, 3D Secure authentication, and proper error handling for declined cards. Return transaction ID on success."
    - "`com.example.strategy.impl.CreditCardStrategy.validatePaymentMethod()` (draft): Implement Luhn algorithm validation for card number, expiry date validation (must be future date), and CVV format check (3-4 digits). Return true only if all validations pass."
    - "`com.example.database.DatabaseConnector.username` (privacy): Database credential - never log or include in error messages"
    - "`com.example.database.DatabaseConnector.password` (privacy): Database credential - never log or include in error messages"
    - "`com.example.service.InventoryService.customerId` (privacy): Customer identifiers linked to purchase history — PII under GDPR"
    - "`com.example.service.NotificationService.sendEmail(java.lang.String,java.lang.String,java.lang.String)` (privacy): Email address is PII under GDPR - never log the recipient address"
    - "`com.example.service.NotificationService.sendSMS(java.lang.String,java.lang.String)` (privacy): Phone number is PII - never log the destination number"
    - "`com.example.service.OrderService.generateOrderConfirmation(java.lang.String)` (privacy): Output contains customer shipping address and contact details (PII)"
    - "`com.example.strategy.impl.CreditCardStrategy.cardNumber` (privacy): PCI-DSS cardholder data - never log or expose in suggestions"
    - "`com.example.strategy.impl.CreditCardStrategy.expiryDate` (privacy): PCI-DSS cardholder data - never log or expose in suggestions"
    - "`com.example.strategy.impl.CreditCardStrategy.cvv` (privacy): PCI-DSS security code - never log or expose in suggestions"
    - "`com.example.security.SecurityConfig` (core, sensitivity: Critical): This is a security manager. Any single-line change can compromise the entire project."
    - "`com.example.service.InventoryService.reserveStock(java.lang.String,int,java.lang.String)` (core, sensitivity: Critical): Reservation logic handles concurrent requests via optimistic locking. Took 18 months to get right under high load — do not refactor without running the full concurrency test suite."
    - "`com.example.service.InventoryService.releaseReservation(java.lang.String)` (core, sensitivity: High): Must be called as the exact inverse of reserveStock. Pair changes to both methods together."
    - "`com.example.payment.PaymentProcessor` (performance): HFT-level requirements: O(1) processing time expected. No database lookups in processing loop."
    - "`com.example.service.InventoryService.getAvailableStock(java.lang.String)` (performance): O(1) lookup required. Must complete in <2ms p99. No database calls permitted; reads from in-memory cache only."
    - "`com.example.service.InventoryService.bulkRestock(java.util.List<java.util.Map<java.lang.String,java.lang.Object>>)` (performance): Must process 10 000 SKU updates/second. O(n) acceptable; O(n log n) only if unavoidable; O(n²) is forbidden."
    - "`com.example.service.PricingService.calculatePrice(java.lang.String,int,java.lang.String)` (performance): Must complete in <5ms p99. Called on every cart update."
    - "`com.example.service.PricingService.calculatePrice(java.lang.String,int,java.lang.String)` (contract): signature frozen — Signature locked by OpenAPI v2 contract. checkout-service and mobile-app bind to this exact signature. A type change is a breaking API change."
    - "`com.example.service.PricingService.applyPromoCode(java.lang.String,double,java.lang.String)` (contract): signature frozen — Promotions-service depends on this exact method signature for its async price-adjustment events. Changing parameter types would break the event deserialization."
    - "`com.example.service.PricingService.getBulkPricing(java.util.List<java.lang.String>,int)` (contract): signature frozen — B2B portal contract v1.2 — the List<Map<String,Object>> structure is serialized directly to JSON. Changing the return type breaks portal parsing."
    - "`com.example.service.OrderService.calculateDiscount(java.lang.String,java.lang.String)` (test-driven): Coverage goal: 100%. Framework: JUNIT_5, ASSERTJ. Mock policy: Use fixed prices — no external pricing calls in unit tests."
    - "`com.example.service.OrderService.updateOrderStatus(java.lang.String,java.lang.String)` (test-driven): Coverage goal: 95%. Framework: JUNIT_5, MOCKITO. Test file: src/test/java/com/example/service/OrderServiceTest.java. Mock policy: Mock OrderRepository and EventPublisher; use real state machine logic."
    - "`com.example.concurrent.SessionCache` (thread-safe): Strategy: LOCK_FREE. Note: All mutations go through ConcurrentHashMap; never introduce a synchronized block on the cache map."
    - "`com.example.config.AsyncTestConfig` (immutable): Used by every test runner; safe to share across threads without copies."
    - "`com.example.legacy.OldPaymentApi` (deprecated): Replaced by: `com.example.payment.PaymentProcessor`. Switch callers to PaymentProcessor.charge(). The new API uses Money instead of double. (Removal deadline: v2.0 (2026-Q4))"
    - "`com.example.metrics.OrderMetrics.recordOrderPlaced(java.lang.String,boolean)` (observability): Metrics: orders.placed.total, orders.placed.failed. Traces: order.place. Logs: OrderPlaced, OrderPlacementFailed. Note: Watched by the Orders SLO dashboard (https://grafana.internal/d/orders-slo)."
    - "`com.example.compliance.GdprService` (regulation): GDPR Art. 17 — Right to erasure — when invoked, deletes ALL PII for the given user across every connected store."
    - "`com.example.compliance.GdprService.exportUserData(java.lang.String)` (regulation): GDPR Art. 20 — Right to data portability — exports the user's data in a machine-readable format."
    - "`com.example.config.ParallelTestSettings` (test-isolation): Strict test isolation required. No shared mutable state or external resource conflicts."
    - "`com.example.legacy.LegacyBridgeService` (legacy-bridge): Legacy/compatibility bridge. Do not refactor structural patterns; only modify internal business logic as explicitly requested."
    - "`com.example.service.LayeredDomainService` (architecture): Belongs to layer: `domain`. Prohibited from referencing: [infrastructure, ui]"
    - "`com.example.service.PublicPaymentController` (public-api): Public API surface. Preserve signature, Javadoc, backwards compatibility, and binary/source stability."
    - "`com.example.service.TransactionalPaymentService` (strict-exceptions): Strict exception handling required. Catching/throwing generic Exception/Throwable is prohibited."
    - "`com.example.payment.PaymentDetails` (strict-types): Loose typing (Object, Map<String, Object>, raw types) is prohibited. Enforce type safety."
    - "`com.example.utils.I18nMessageHelper` (i18n): Internationalization mandated. User-facing strings must not be hardcoded; retrieve from resources."
    - "`com.example.utils.StrictUtility` (strict-classpath): Strict compile-time dependency/classpath constraints. Dynamic loading and reflection hacks prohibited."
    - "`com.example.database.UserEntity` (schema-safe): Schema/serialization safety guaranteed. Prohibit altering data formats or fields without migration plan."
    - "`com.example.compliance.GdprService.deleteAllUserData(java.lang.String)` (idempotent): Idempotency guaranteed. Multiple invocations must produce the same result as one. Reason: Deleting a user's data multiple times must produce the same result as deleting once — must not throw on second invocation."
    - "`com.example.service.InventoryService.sendLowStockAlert(java.lang.String,int)` (feature-flag): Gated by feature flag: 'inventory.push-alerts.enabled' (default: false). Preserve the flag check — never assume it is always on."
    - "`com.example.security.SecurityConfig` (security-critical): Security-critical code [authentication]. Do not weaken security properties. Flag any change for security review."
# VIBETAGS-END
