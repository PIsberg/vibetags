<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>se.deversity.vibetags.example</groupId>
    <artifactId>vibetags-example</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>VibeTags Example Project</name>
    <description>Example project demonstrating VibeTags AI guardrails library</description>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- BOM is the single source of truth for vibetags-* versions. Bumping this one
             value rolls the processor and any future vibetags-* artifact in lockstep. -->
        <vibetags.bom.version>1.3.1</vibetags.bom.version>
        <vibetags.log.path>vibetags.log</vibetags.log.path>
      <!-- The diagnostic channel. INFO reports what ran; `mvn compile
         -Dvibetags.log.level=DEBUG` adds one structured event per decision to vibetags.log,
         which is how you find out why a file was or was not rewritten. -->
    <vibetags.log.level>INFO</vibetags.log.level>
    <!-- The per-file write cache (.vibetags-cache). `mvn clean compile -Dvibetags.cache=false`
         regenerates with the cache disabled; CI does exactly that and asserts the output is
         byte-identical. -->
    <vibetags.cache>true</vibetags.cache>
  </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>se.deversity.vibetags</groupId>
                <artifactId>vibetags-bom</artifactId>
                <version>${vibetags.bom.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Annotations only on the compile classpath. The processor itself lives on
             the annotation-processor path (configured below) so its slf4j/logback
             baggage does not leak into compileClasspath. Versions come from the BOM. -->
        <dependency>
            <groupId>se.deversity.vibetags</groupId>
            <artifactId>vibetags-annotations</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
                <configuration>
                    <release>21</release>
                    <compilerArgs>
                        <arg>-Avibetags.root=${project.basedir}</arg>
                        <arg>-Avibetags.log.path=${vibetags.log.path}</arg>
                      <arg>-Avibetags.log.level=${vibetags.log.level}</arg>
                        <arg>-Avibetags.cache=${vibetags.cache}</arg>
            </compilerArgs>
                    <!-- Explicit processor path required for Java 23+ where implicit classpath
                         discovery is disabled by default (see javac release 23 notes). -->
                    <!-- annotationProcessorPaths does not inherit versions from
                         dependencyManagement (Maven plugin limitation, MCOMPILER-391),
                         so the BOM-managed version is repeated here via the same
                         property. Updating vibetags.bom.version above keeps them in sync. -->
                    <annotationProcessorPaths>
                        <path>
                            <groupId>se.deversity.vibetags</groupId>
                            <artifactId>vibetags-processor</artifactId>
                            <version>${vibetags.bom.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
