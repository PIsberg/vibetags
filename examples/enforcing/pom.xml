<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>se.deversity.vibetags.example</groupId>
    <artifactId>vibetags-example-enforcing</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>VibeTags Enforcing-Mode Example</name>
    <description>Consumer project demonstrating -Avibetags.enforce and the committed baseline</description>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- BOM is the single source of truth for vibetags-* versions. -->
        <vibetags.bom.version>1.3.0</vibetags.bom.version>
        <!-- Enforcement is ON for every build of this example: the three provable families
             must match the committed .vibetags-baseline or compilation fails. That is the
             point of the example, so it is not hidden behind a profile. -->
        <vibetags.enforce>locked,contract,publicapi</vibetags.enforce>
        <!-- Flipped per invocation to (re)record the baseline after an approved shape change:
                 mvn compile -Dvibetags.baseline.update=true
             Recording replaces checking for that one build, so the very next plain build
             enforces the shapes you just approved. -->
        <vibetags.baseline.update>false</vibetags.baseline.update>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>se.deversity.vibetags</groupId>
                <artifactId>vibetags-bom</artifactId>
                <version>${vibetags.bom.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>se.deversity.vibetags</groupId>
            <artifactId>vibetags-annotations</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
                <configuration>
                    <release>21</release>
                    <compilerArgs>
                        <arg>-Avibetags.root=${project.basedir}</arg>
                        <arg>-Avibetags.enforce=${vibetags.enforce}</arg>
                        <arg>-Avibetags.baseline.update=${vibetags.baseline.update}</arg>
                    </compilerArgs>
                    <!-- Explicit processor path required for Java 23+ where implicit classpath
                         discovery is disabled by default. annotationProcessorPaths does not
                         inherit versions from dependencyManagement (MCOMPILER-391), so the
                         BOM-managed version is repeated via the same property. -->
                    <annotationProcessorPaths>
                        <path>
                            <groupId>se.deversity.vibetags</groupId>
                            <artifactId>vibetags-processor</artifactId>
                            <version>${vibetags.bom.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
