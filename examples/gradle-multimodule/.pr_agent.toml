# AUTO-GENERATED BY VIBETAGS
# Generated by VibeTags | https://github.com/PIsberg/vibetags
# Qodo/Codium PR-Agent configuration. Do not edit manually.

[pr_reviewer]
extra_instructions = """
Enforce the following VibeTags guardrails. Flag any change that violates them:
- `com.example.gmm.app.App` (context): Focus - Wiring only: parsing and rendering live in their own modules. Avoid - Business logic
- `com.example.gmm.app.App` (audit): check for Path Traversal
- `com.example.gmm.core.IrNode` (locked): Core IR node shape is depended on by every downstream module
- `com.example.gmm.core` (context): Focus - Immutable IR data model shared across every module of the reactor.. Avoid - Adding mutable state, framework annotations, or a dependency on any sibling module.
- `com.example.gmm.core` (thread-safe): Strategy: IMMUTABLE. Note: Every type in this package is safe to publish across threads without synchronization.
- `com.example.gmm.core` (security-critical): Security-critical code [Node identity is a security boundary: never build an IrNode from unvalidated external input, and never expose its raw name in a URL or log line.]. Do not weaken security properties. Flag any change for security review.
- `com.example.gmm.platform.Telemetry` (locked): Metric names are a published contract; renaming one breaks every dashboard and alert reading them
- `com.example.gmm.platform.Telemetry` (observability): Metrics: reactor.render.count, reactor.render.duration. Note: Metric names are a published contract; renaming one breaks every dashboard reading it
- `com.example.gmm.platform.FeatureGate` (feature-flag): Gated by feature flag: 'reactor.parallel-render' (default: false). Preserve the flag check — never assume it is always on.
"""

[pr_code_suggestions]
extra_instructions = """
Enforce the following VibeTags guardrails. Flag any change that violates them:
- `com.example.gmm.app.App` (context): Focus - Wiring only: parsing and rendering live in their own modules. Avoid - Business logic
- `com.example.gmm.app.App` (audit): check for Path Traversal
- `com.example.gmm.core.IrNode` (locked): Core IR node shape is depended on by every downstream module
- `com.example.gmm.core` (context): Focus - Immutable IR data model shared across every module of the reactor.. Avoid - Adding mutable state, framework annotations, or a dependency on any sibling module.
- `com.example.gmm.core` (thread-safe): Strategy: IMMUTABLE. Note: Every type in this package is safe to publish across threads without synchronization.
- `com.example.gmm.core` (security-critical): Security-critical code [Node identity is a security boundary: never build an IrNode from unvalidated external input, and never expose its raw name in a URL or log line.]. Do not weaken security properties. Flag any change for security review.
- `com.example.gmm.platform.Telemetry` (locked): Metric names are a published contract; renaming one breaks every dashboard and alert reading them
- `com.example.gmm.platform.Telemetry` (observability): Metrics: reactor.render.count, reactor.render.duration. Note: Metric names are a published contract; renaming one breaks every dashboard reading it
- `com.example.gmm.platform.FeatureGate` (feature-flag): Gated by feature flag: 'reactor.parallel-render' (default: false). Preserve the flag check — never assume it is always on.
"""
