// Deliberately minimal: its existence is what makes VibeTags treat this directory as a
// module root rather than folding it into the root project. See ../../gradle-shared-buildfile for the
// variant where this file is absent.
