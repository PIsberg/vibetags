# VIBETAGS-START
# AUTO-GENERATED BY VIBETAGS
# Generated by VibeTags | https://github.com/PIsberg/vibetags
# Sweep AI code review rules. Do not edit manually.

rules:
  # VIBETAGS-MODULE: app
  - "Security audit required for com.example.gmm.app.App: Path Traversal"
  # VIBETAGS-MODULE-END: app
  # VIBETAGS-MODULE: core
  - "Do not modify com.example.gmm.core.IrNode: Core IR node shape is depended on by every downstream module"
  - "Security-critical: com.example.gmm.core [Node identity is a security boundary: never build an IrNode from unvalidated external input, and never expose its raw name in a URL or log line.]. Do not weaken security."
  # VIBETAGS-MODULE-END: core
  # VIBETAGS-MODULE: platform
  - "Security audit required for com.example.gmm.platform.Telemetry: PII in metric labels"
  - "Feature flag gate for com.example.gmm.platform.FeatureGate: Gated by feature flag: 'reactor.parallel-render' (default: false). Preserve the flag check — never assume it is always on."
  # VIBETAGS-MODULE-END: platform
# VIBETAGS-END
