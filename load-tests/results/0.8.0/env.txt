Processor version: 0.8.0
Date (UTC): 2026-05-06T23:45:00Z
OS: Microsoft Windows NT 10.0.26200.0
CPU: 12th Gen Intel(R) Core(TM) i7-1260P

JDK:
Picked up JAVA_TOOL_OPTIONS: -Dfile.encoding=UTF8
java version "26" 2026-03-17
Java(TM) SE Runtime Environment (build 26+35-2893)
Java HotSpot(TM) 64-Bit Server VM (build 26+35-2893, mixed mode, sharing)

Stress sweep cap: stress.max.classes=1000

New in 0.8.0:

  1. @AITestDriven annotation — enforces Red-Green-Refactor TDD workflow. AI must
     provide test code alongside any proposed change; a change without matching
     tests is treated as incomplete. Attributes: framework (enum array, default
     JUNIT_5), coverageGoal (0–100, default 100), testLocation (explicit test
     file path), mockPolicy (external-dependency handling instruction).
     Adds a <test_driven_requirements> section to all platform output files.

  2. 7 new AI platform integrations:
       .pearai/rules/*.md          (PearAI — granular per-class, YAML front-matter)
       .mentatconfig.json          (Mentat — JSON config)
       sweep.yaml                  (Sweep GitHub App — YAML rules list)
       .plandex.yaml               (Plandex — YAML guardrails)
       .doubleignore               (Double.bot — glob patterns)
       .interpreter/profiles/vibetags.yaml  (Open Interpreter — YAML profile)
       .codeiumignore              (Codeium — glob patterns)

Caveats — read before drawing conclusions:

- The JMH suite calls writeFileIfChanged directly, bypassing
  GuardrailContentBuilder. Platform additions add zero JMH overhead.
- The stress sweep creates a fresh TempDir per leg → cache is always empty;
  numbers reflect Windows process-launch jitter ±15%.
- The smallWrite benchmark mutates the file before each iteration so the cache
  always misses. writeFileIfChanged_smallWrite and _noChange are comparable to
  0.7.1 (within noise) — the new annotation and platforms add zero I/O overhead
  for projects that do not opt in.
- The stress numbers at N=500 and N=1000 are lower overhead than 0.7.1 for this
  run, consistent with historical variance documented in the 0.7.1 env.txt.
- OutputSize(B) is unchanged from 0.7.1 at every N for the default opt-in set
  (the stress test opts in to .cursorrules, CLAUDE.md, .aiexclude, AGENTS.md,
  gemini_instructions.md, QWEN.md — none of the new platforms).
