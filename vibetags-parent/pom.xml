<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>se.deversity.vibetags</groupId>
    <artifactId>vibetags-parent</artifactId>
    <version>${revision}</version>
    <packaging>pom</packaging>

    <name>VibeTags Parent</name>
    <description>
        Shared build configuration for the VibeTags subprojects: every version, every plugin
        declaration, and the publishing setup. Not published to Maven Central — the
        flatten-maven-plugin resolves this parent away before deploy, so the POMs consumers
        download stay self-contained and gain no new artifact to resolve.
    </description>
    <!--
      inherit.append.path="false" on every inherited URL. Maven's default is to append the child's
      artifactId to the parent's url and scm entries, which for a single-repository project is
      simply wrong: without these attributes the published POMs claimed
      scm:git:git://github.com/PIsberg/vibetags.git/vibetags-annotations and a project url of
      .../vibetags/vibetags-annotations, neither of which exists. Maven Central would have accepted
      both, and the SCM link on every artifact page would have pointed nowhere.
    -->
    <url child.project.url.inherit.append.path="false">https://github.com/PIsberg/vibetags</url>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>PIsberg</id>
            <name>Peter Isberg</name>
            <url>https://github.com/PIsberg</url>
        </developer>
    </developers>

    <scm child.scm.connection.inherit.append.path="false"
         child.scm.developerConnection.inherit.append.path="false"
         child.scm.url.inherit.append.path="false">
        <connection>scm:git:git://github.com/PIsberg/vibetags.git</connection>
        <developerConnection>scm:git:ssh://github.com/PIsberg/vibetags.git</developerConnection>
        <url>https://github.com/PIsberg/vibetags/tree/main</url>
    </scm>

    <!--
      ==========================================================================
      THE ONE PLACE. Every version in the build is declared here and nowhere
      else. A subproject that writes a literal version is a subproject that will
      be forgotten at the next bump, which is not hypothetical: before this file
      existed, load-tests sat on maven-compiler-plugin 3.13.0 while six other
      poms were on 3.15.0, on surefire 3.2.5 against vibetags' 3.5.6, and
      declared junit.version=5.10.2 under the same property name vibetags used
      for 6.1.2.

      BuildVersionParityTest fails the build when a version literal reappears in
      a managed pom, or when the Gradle files (which cannot inherit from here)
      drift from these values.
      ==========================================================================
    -->
    <properties>
        <!-- The VibeTags release version. Bump THIS and nothing else: every module takes its
             own version, its sibling dependencies and its BOM entries from it. Resolved into
             the deployed POMs by flatten-maven-plugin, so consumers never see the property. -->
        <revision>1.2.5</revision>

        <!-- release, not source/target: on a JDK newer than 21 (CI builds 21, 25 and 26)
             source/target compiles against the *running* JDK's API and javac warns that the
             result may not run on 21. release pins the API to 21 and the warning goes away. -->
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Third-party runtime/compile dependencies -->
        <jspecify.version>1.0.1</jspecify.version>
        <slf4j.version>2.0.18</slf4j.version>
        <logback.version>1.6.3</logback.version>

        <!-- Test dependencies. Keep in step with vibetags/build.gradle and
             load-tests/pom.xml; BuildVersionParityTest enforces it. -->
        <junit.version>6.1.3</junit.version>
        <junit.platform.version>6.1.3</junit.platform.version>
        <mockito.version>5.23.0</mockito.version>
        <archunit.version>1.5.0</archunit.version>
        <async-test-lib.version>1.9.8</async-test-lib.version>
        <snakeyaml.version>2.6</snakeyaml.version>
        <jmh.version>1.37</jmh.version>

        <!-- Verification-only tooling: never on any module's classpath. ECJ is how CI checks
             the documented claim that the processor degrades rather than fails under a compiler
             with no Tree API (docs/PROCESSOR.md, USAGE.md). scripts/ecj-degradation-check.sh
             reads this property so the version has one home, like every other pin here. -->
        <ecj.version>3.46.0</ecj.version>

        <!-- Build plugins -->
        <maven-compiler-plugin.version>3.15.0</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.5.6</maven-surefire-plugin.version>
        <maven-jar-plugin.version>3.5.1</maven-jar-plugin.version>
        <maven-source-plugin.version>3.4.0</maven-source-plugin.version>
        <maven-javadoc-plugin.version>3.12.0</maven-javadoc-plugin.version>
        <maven-shade-plugin.version>3.6.2</maven-shade-plugin.version>
        <exec-maven-plugin.version>3.6.3</exec-maven-plugin.version>
        <flatten-maven-plugin.version>1.8.0</flatten-maven-plugin.version>

        <!-- Static analysis and coverage -->
        <maven-pmd-plugin.version>3.28.0</maven-pmd-plugin.version>
        <pmd.version>7.26.0</pmd.version>
        <maven-checkstyle-plugin.version>3.6.0</maven-checkstyle-plugin.version>
        <spotbugs-maven-plugin.version>4.10.4.0</spotbugs-maven-plugin.version>
        <maven-enforcer-plugin.version>3.6.3</maven-enforcer-plugin.version>
        <findsecbugs-plugin.version>1.14.0</findsecbugs-plugin.version>
        <jacoco-maven-plugin.version>0.8.15</jacoco-maven-plugin.version>
        <pitest-maven.version>1.25.9</pitest-maven.version>
        <pitest-junit5-plugin.version>1.2.3</pitest-junit5-plugin.version>
        <error-prone.version>2.50.0</error-prone.version>
        <nullaway.version>0.14.0</nullaway.version>
        <cyclonedx-maven-plugin.version>2.9.3</cyclonedx-maven-plugin.version>

        <!-- Publishing -->
        <central-publishing-maven-plugin.version>0.11.0</central-publishing-maven-plugin.version>
        <maven-gpg-plugin.version>3.2.8</maven-gpg-plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>se.deversity.vibetags</groupId>
                <artifactId>vibetags-annotations</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- Sibling coordinate for vibetags-cli, which consumes the processor as a
                 library (ServiceRegistry, marker constants). -->
            <dependency>
                <groupId>se.deversity.vibetags</groupId>
                <artifactId>vibetags-processor</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>org.jspecify</groupId>
                <artifactId>jspecify</artifactId>
                <version>${jspecify.version}</version>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>${logback.version}</version>
            </dependency>

            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.junit.platform</groupId>
                <artifactId>junit-platform-launcher</artifactId>
                <version>${junit.platform.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-core</artifactId>
                <version>${mockito.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-junit-jupiter</artifactId>
                <version>${mockito.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>com.tngtech.archunit</groupId>
                <artifactId>archunit-junit5</artifactId>
                <version>${archunit.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>se.deversity.async-test-lib</groupId>
                <artifactId>async-test-lib</artifactId>
                <version>${async-test-lib.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.yaml</groupId>
                <artifactId>snakeyaml</artifactId>
                <version>${snakeyaml.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.openjdk.jmh</groupId>
                <artifactId>jmh-core</artifactId>
                <version>${jmh.version}</version>
            </dependency>
            <dependency>
                <groupId>org.openjdk.jmh</groupId>
                <artifactId>jmh-generator-annprocess</artifactId>
                <version>${jmh.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven-jar-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven-source-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${maven-javadoc-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-shade-plugin</artifactId>
                    <version>${maven-shade-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>exec-maven-plugin</artifactId>
                    <version>${exec-maven-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-pmd-plugin</artifactId>
                    <version>${maven-pmd-plugin.version}</version>
                    <dependencies>
                        <dependency>
                            <groupId>net.sourceforge.pmd</groupId>
                            <artifactId>pmd-core</artifactId>
                            <version>${pmd.version}</version>
                        </dependency>
                        <dependency>
                            <groupId>net.sourceforge.pmd</groupId>
                            <artifactId>pmd-java</artifactId>
                            <version>${pmd.version}</version>
                        </dependency>
                    </dependencies>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-checkstyle-plugin</artifactId>
                    <version>${maven-checkstyle-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>com.github.spotbugs</groupId>
                    <artifactId>spotbugs-maven-plugin</artifactId>
                    <version>${spotbugs-maven-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco-maven-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.pitest</groupId>
                    <artifactId>pitest-maven</artifactId>
                    <version>${pitest-maven.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.cyclonedx</groupId>
                    <artifactId>cyclonedx-maven-plugin</artifactId>
                    <version>${cyclonedx-maven-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.sonatype.central</groupId>
                    <artifactId>central-publishing-maven-plugin</artifactId>
                    <version>${central-publishing-maven-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-gpg-plugin</artifactId>
                    <version>${maven-gpg-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>flatten-maven-plugin</artifactId>
                    <version>${flatten-maven-plugin.version}</version>
                </plugin>
                <!--
                  Supply-chain gate for the dependency surface that ships. Everything on the
                  compile/runtime classpath of the published artifacts is allowlisted here;
                  docs/DEPENDENCIES.md is the human ledger and this rule is its executable
                  form - policy the build does not read is a comment. Test-scoped and
                  provided-scoped dependencies are exempt: they never reach a consumer, and
                  gating them would make every test-library bump a two-file change for no
                  consumer-facing risk reduction.

                  A new shipping dependency therefore enters this project by editing this
                  list, in a reviewed diff, never by an agent resolving one mid-task. The
                  modules that publish artifacts (vibetags-annotations, vibetags,
                  vibetags-cli) activate the plugin; load-tests and the examples do not,
                  because their dependency surface is a workbench, not a shipment.
                -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-enforcer-plugin</artifactId>
                    <version>${maven-enforcer-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>enforce-shipping-dependency-allowlist</id>
                            <goals>
                                <goal>enforce</goal>
                            </goals>
                            <configuration>
                                <rules>
                                    <bannedDependencies>
                                        <excludes>
                                            <exclude>*</exclude>
                                        </excludes>
                                        <includes>
                                            <include>*:*:*:*:test</include>
                                            <include>*:*:*:*:provided</include>
                                            <include>se.deversity.vibetags:*</include>
                                            <include>org.jspecify:jspecify</include>
                                            <include>org.slf4j:slf4j-api</include>
                                            <include>ch.qos.logback:*</include>
                                        </includes>
                                        <searchTransitive>true</searchTransitive>
                                        <message>A dependency outside the shipping allowlist reached the compile/runtime classpath. If it belongs here, add it to the allowlist in vibetags-parent/pom.xml and record it in docs/DEPENDENCIES.md, in the same reviewed change.</message>
                                    </bannedDependencies>
                                </rules>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <!--
              Resolves ${revision} and strips this parent out of the POM that gets installed and
              deployed. Without it, `<version>${revision}</version>` would reach Maven Central as a
              literal property reference and every consumer build would fail to resolve it.

              flattenMode=ossrh keeps exactly the elements Central requires (name, description,
              url, licenses, developers, scm) and drops the build section, so what consumers
              download describes the artifact and nothing about how it was built.
            -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <configuration>
                    <updatePomFile>true</updatePomFile>
                    <flattenMode>ossrh</flattenMode>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>flatten.clean</id>
                        <phase>clean</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <distributionManagement>
        <snapshotRepository>
            <id>central</id>
            <url>https://central.sonatype.com/repository/maven-snapshots/</url>
        </snapshotRepository>
        <repository>
            <id>central</id>
            <url>https://central.sonatype.org/service/local/staging/deploy/maven2/</url>
        </repository>
    </distributionManagement>

    <profiles>
        <profile>
            <id>central-publish</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>true</autoPublish>
                            <waitUntil>published</waitUntil>
                            <!-- The default gave up on vibetags-cli 1.2.1 after 30 minutes, with
                                 the bundle uploaded and still validating. The deploy is then
                                 neither done nor undone: the job fails, every later module is
                                 skipped, and the artifact may publish itself minutes later.
                                 Central's own validation is occasionally slow rather than stuck,
                                 so wait an hour before calling it a failure. -->
                            <waitMaxTime>3600</waitMaxTime>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>sign-artifacts</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                                <configuration>
                                    <gpgArguments>
                                        <arg>--pinentry-mode</arg>
                                        <arg>loopback</arg>
                                    </gpgArguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
