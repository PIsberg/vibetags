/**
 * Per-annotation formatter implementations.
 *
 * <p>Each class in this package formats a single annotation type for one or
 * more target platforms. All types are {@code @NullMarked}: every parameter
 * and return type is non-null by default.
 */
@NullMarked
package se.deversity.vibetags.processor.internal.content.annotations;

import org.jspecify.annotations.NullMarked;
