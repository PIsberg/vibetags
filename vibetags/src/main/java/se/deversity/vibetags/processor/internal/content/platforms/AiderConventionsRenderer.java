package se.deversity.vibetags.processor.internal.content.platforms;

import se.deversity.vibetags.processor.model.TaggedElement;
import se.deversity.vibetags.processor.model.GuardrailModel;
import se.deversity.vibetags.processor.internal.content.FormatterRegistry;
import se.deversity.vibetags.processor.internal.content.Platform;
import se.deversity.vibetags.processor.internal.content.PlatformRenderer;
import se.deversity.vibetags.processor.internal.content.RenderingContext;

/**
 * PlatformRenderer for generating `CONVENTIONS.md` for Aider.
 */
public final class AiderConventionsRenderer implements PlatformRenderer {
    @Override
    public String render(GuardrailModel model, Platform platform, RenderingContext context) {
        StringBuilder sb = new StringBuilder(context.estimatedContentSize());
        sb.append("# ").append(context.getProjectName()).append(" CONVENTIONS\n")
          .append(context.getGeneratedHeader())
          .append("# AUTO-GENERATED BY VIBETAGS\n\n")
          .append("This file contains project-specific coding conventions and AI guardrails extracted from source annotations.\n\n");

        for (TaggedElement e : model.locked()) {
            FormatterRegistry.locked().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.context()) {
            FormatterRegistry.context().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.ignore()) {
            FormatterRegistry.ignore().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.audit()) {
            FormatterRegistry.audit().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.draft()) {
            FormatterRegistry.draft().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.privacy()) {
            FormatterRegistry.privacy().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.core()) {
            FormatterRegistry.core().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.performance()) {
            FormatterRegistry.performance().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.contract()) {
            FormatterRegistry.contract().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.testDriven()) {
            FormatterRegistry.testDriven().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.threadSafe()) {
            FormatterRegistry.threadSafe().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.immutable()) {
            FormatterRegistry.immutable().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.deprecated()) {
            FormatterRegistry.deprecated().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.observability()) {
            FormatterRegistry.observability().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.regulation()) {
            FormatterRegistry.regulation().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.parallelTests()) {
            FormatterRegistry.parallelTests().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.legacyBridge()) {
            FormatterRegistry.legacyBridge().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.architecture()) {
            FormatterRegistry.architecture().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.publicApi()) {
            FormatterRegistry.publicApi().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.strictExceptions()) {
            FormatterRegistry.strictExceptions().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.strictTypes()) {
            FormatterRegistry.strictTypes().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.internationalized()) {
            FormatterRegistry.internationalized().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.strictClasspath()) {
            FormatterRegistry.strictClasspath().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.schemaSafe()) {
            FormatterRegistry.schemaSafe().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.idempotent()) {
            FormatterRegistry.idempotent().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.featureFlag()) {
            FormatterRegistry.featureFlag().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.secure()) {
            FormatterRegistry.secure().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.callersOnly()) {
            FormatterRegistry.callersOnly().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.sandboxOnly()) {
            FormatterRegistry.sandboxOnly().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.memoryBudget()) {
            FormatterRegistry.memoryBudget().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.pure()) {
            FormatterRegistry.pure().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.domainModel()) {
            FormatterRegistry.domainModel().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.extensible()) {
            FormatterRegistry.extensible().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.inputSanitized()) {
            FormatterRegistry.inputSanitized().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.secureLogging()) {
            FormatterRegistry.secureLogging().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.explain()) {
            FormatterRegistry.explain().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.prototype()) {
            FormatterRegistry.prototype().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.sunset()) {
            FormatterRegistry.sunset().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.temporary()) {
            FormatterRegistry.temporary().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.generated()) {
            FormatterRegistry.generated().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.loadBearing()) {
            FormatterRegistry.loadBearing().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.bannedApi()) {
            FormatterRegistry.bannedApi().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.threadAffinity()) {
            FormatterRegistry.threadAffinity().format(e, sb, Platform.AIDER_CONVENTIONS);
        }
        for (TaggedElement e : model.keepInSync()) {
            FormatterRegistry.keepInSync().format(e, sb, Platform.AIDER_CONVENTIONS);
        }

        return sb.toString();
    }
}
