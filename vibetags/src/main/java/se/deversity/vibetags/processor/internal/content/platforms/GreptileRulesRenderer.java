package se.deversity.vibetags.processor.internal.content.platforms;

import se.deversity.vibetags.processor.model.GuardrailModel;
import se.deversity.vibetags.processor.internal.content.Platform;
import se.deversity.vibetags.processor.internal.content.PlatformRenderer;
import se.deversity.vibetags.processor.internal.content.RenderingContext;

/**
 * PlatformRenderer for Greptile's {@code .greptile/rules.md}.
 *
 * <p>Greptile documents the {@code .greptile/} folder as its recommended configuration and the root
 * {@code greptile.json} as the legacy form. {@code rules.md} is plain Markdown passed to the reviewer
 * with no parsing, so it takes HTML-comment markers like any other Markdown output and needs none of
 * the key-merge machinery {@code greptile.json} does.
 */
public final class GreptileRulesRenderer implements PlatformRenderer {

    @Override
    public String render(GuardrailModel model, Platform platform, RenderingContext context) {
        StringBuilder sb = new StringBuilder(context.estimatedContentSize());
        sb.append("# ").append(context.getProjectName()).append(" review rules\n")
          .append(context.getGeneratedHeader())
          .append("# AUTO-GENERATED BY VIBETAGS. Greptile reads this file when reviewing a ")
          .append("pull request in this repository.\n\n")
          .append("## VibeTags guardrails\n\n");

        String block = GuardrailInstructionBlock.build(model);
        if (block.isBlank()) {
            sb.append("No VibeTags guardrails are currently declared.\n");
        } else {
            sb.append("Flag any change in this pull request that violates one of the following:\n\n")
              .append(block.strip()).append('\n');
        }
        return sb.toString();
    }
}
