/**
 * Internal implementation classes for the VibeTags annotation processor.
 *
 * <p>Classes in this package are not part of the public API and may change at
 * any time without notice. All types are {@code @NullMarked}: every parameter
 * and return type is non-null by default; exceptions are annotated with
 * {@code @Nullable} explicitly.
 */
@NullMarked
package se.deversity.vibetags.processor.internal;

import org.jspecify.annotations.NullMarked;
